[project]
name = "sanskritix"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "google-genai",
    "python-dotenv"
]

[tool.vercel]
entrypoint = "backend.app:app"
