# Copy this file to .env before running Gemini analysis locally.
# Never commit the real .env file or a real API key.
GEMINI_API_KEY=your_api_key_here

# This can be changed later without editing Python code.
GEMINI_MODEL=gemini-3.6-flash
